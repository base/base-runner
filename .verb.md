## Usage

```js
var runner = require('{%= name %}');
var Base = require('base');
var base = new Base();
base.use(runner());
```

## API
{%= apidocs("index.js") %}

## CLI
{%= apidocs("lib/commands/*.js") %}
